<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submarine"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- 🚀 Écran principal : MainActivity (point d'entrée de l'app) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Submarine">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🟢 Écran d’authentification (Login/Signup combiné) -->
        <activity
            android:name=".auth.Signup"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Submarine" />

        <!-- 🟣 Édition de bio -->
        <activity
            android:name=".bio.EditBioActivity"
            android:exported="true" />

        <!-- 🗨️ Conversation / Chat -->
        <activity android:name=".conversation.ConversationActivity" />

        <!-- 👥 Contacts -->
        <activity android:name=".contacts.ContactsActivity" />

        <!-- ➕ Ajouter un contact -->
        <activity
            android:name=".listeContact.AddContactActivity"
            android:exported="false"
            android:label="Ajouter un contact"
            android:theme="@style/Theme.Submarine" />

        <activity  android:name=".admin.AdminActivity"
            android:exported="false"
            android:label="Administration" />
    </application>

    <!-- 🌐 Autorisations réseau -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>
